# Template for gui_secrets.yaml — copy to gui_secrets.yaml and fill in.
# gui_secrets.yaml is gitignored. Deep-merged on top of gui_params.yaml at load time.
# Override path: set AGX_ARM_SECRETS env var to point at a custom copy.

spb_bridge:
  local:
    host: localhost
    username: ""
    password: ""

  hivemq:
    host: ""           # e.g. abc123.s1.eu.hivemq.cloud
    username: ""
    password: ""
